Real HTML fixtures captured via web_fetch during this session, used to test the
audit scripts against genuine site content (bash_tool's network is restricted
to package registries, so it can't crawl jntukmaterials.com directly --
these fixtures stand in for a live crawl while proving the parsing logic).
